server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vspring_db
    username: root
    password: sdfsdf
    driver-class-name: com.mysql.cj.jdbc.Driver

  mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.example.vspring.model