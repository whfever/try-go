server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vplusspring_db
    username: root
    password: sdfsdf
    driver-class-name: com.mysql.cj.jdbc.Driver

  mybatis-plus:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.example.vplusspring.model